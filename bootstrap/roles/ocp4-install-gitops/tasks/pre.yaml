## Prerequisites for install CICD Infra

- name: Add ClusterRoleBinding to the acm-hub/default SA
  kubernetes.core.k8s:
    state: present
    definition: "{{ lookup('template', 'acm-hub-crb.yml.j2') }}"

- name: Create Namespaces
  kubernetes.core.k8s:
    state: present
    definition: "{{ lookup('template', 'namespaces.yaml.j2') }}"

